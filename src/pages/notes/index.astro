---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ContentCard from '../../components/ui/ContentCard.astro';
import PageHeader from '../../components/ui/PageHeader.astro';
import { getCollection } from 'astro:content';

const all    = await getCollection('notes');
const sorted = all.sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());
---
<BaseLayout title="Notes">
  <div class="page-container animate-fade-up">
    <PageHeader title="Notes" description="Working notes, reading summaries, and math scratch-work." count={all.length} accent="cyan" />
    <div class="stack stack--md">
      {sorted.map(item => (
        <ContentCard
          href={'/notes/' + item.id}
          title={item.data.title}
          date={item.data.date}
          summary={item.data.summary}
          tags={item.data.tags}
        />
      ))}
      {all.length === 0 && <div class="empty-state">No entries yet. Add .mdx files to src/content/notes/</div>}
    </div>
  </div>
</BaseLayout>
