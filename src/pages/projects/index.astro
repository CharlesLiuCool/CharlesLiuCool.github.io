---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ContentCard from '../../components/ui/ContentCard.astro';
import PageHeader from '../../components/ui/PageHeader.astro';
import { getCollection } from 'astro:content';

const all    = await getCollection('projects');
const sorted = all.sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());
---
<BaseLayout title="Projects">
  <div class="page-container animate-fade-up">
    <PageHeader title="Projects" description="Research prototypes, tools, and implementations." count={all.length} accent="blue" />
    <div class="stack stack--md">
      {sorted.map(item => (
        <ContentCard
          href={'/projects/' + item.id}
          title={item.data.title}
          date={item.data.date}
          authors={item.data.authors}
          summary={item.data.summary}
          tags={item.data.tags}
        />
      ))}
      {all.length === 0 && <div class="empty-state">No entries yet. Add .mdx files to src/content/projects/</div>}
    </div>
  </div>
</BaseLayout>
