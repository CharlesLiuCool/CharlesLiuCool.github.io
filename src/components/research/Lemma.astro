---
interface Props { title?: string; }
const { title } = Astro.props;
---
<div class="math-block math-block--lemma not-prose">
  <div class="math-block__label">Lemma{title ? ` — ${title}` : ''}</div>
  <div class="math-block__body"><slot /></div>
</div>
